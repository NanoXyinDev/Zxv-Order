const $=id=>document.getElementById(id);
async function api(url,opt={}){const r=await fetch(url,{credentials:"same-origin",...opt,headers:{"Content-Type":"application/json",...(opt.headers||{})}});let j={};try{j=await r.json()}catch{}if(!r.ok)throw new Error(j.error||"Request failed");return j}
async function boot(){try{const j=await api("/api/admin/me",{headers:{}});showPanel(j.user)}catch{showLogin()}}
function showLogin(){$("loginView").classList.remove("hidden");$("panelView").classList.add("hidden")}
function showPanel(user){$("loginView").classList.add("hidden");$("panelView").classList.remove("hidden");list()}
$("loginForm").addEventListener("submit",async e=>{e.preventDefault();$("loginOut").textContent="Signing in…";try{const j=await api("/api/admin/login",{method:"POST",body:JSON.stringify({username:$("loginUser").value,password:$("loginPass").value})});$("loginOut").textContent="";showPanel(j.user)}catch(e){$("loginOut").textContent=e.message}})
$("vpsForm").addEventListener("submit",async e=>{e.preventDefault();$("out").textContent="Memverifikasi koneksi SSH…";const body={ip:$("ip").value.trim(),username:$("user").value.trim(),port:Number($("port").value),password:$("pw").value,price:Number($("price").value||0)};try{const j=await api("/api/vps/check",{method:"POST",body:JSON.stringify(body)});$("out").textContent=j.message;$("vpsForm").reset();$("port").value=22;list()}catch(e){$("out").textContent=e.message}})
async function list(){try{const j=await api("/api/vps/list",{headers:{}});$("stock").innerHTML=(j.data||[]).map(x=>`<div class="stock-row"><b>${esc(x.ip)}</b><span>${esc(x.username)}:${x.ssh_port}</span><small>${esc(x.status)}</small><b>Rp ${Number(x.price||0).toLocaleString("id-ID")}</b></div>`).join("")||'<div class="empty">Stock kosong.</div>'}catch(e){$("stock").innerHTML=`<div class="empty">${esc(e.message)}</div>`}}
async function logout(){await api("/api/admin/logout",{method:"POST"});showLogin()}
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
boot();