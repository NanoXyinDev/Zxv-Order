const {sql,initDb,requireSession}=require('../../lib');module.exports=async(req,res)=>{try{await initDb();const u=await requireSession(req);if(!u||!['owner','admin'].includes(u.role))return res.status(401).json({error:'Unauthorized'});const {rows}=await sql`SELECT id,ip,username,ssh_port,price,status,created_at FROM vps_stock ORDER BY id DESC`;res.json({ok:true,data:rows.map(x=>({...x,price:Number(x.price)}))})}catch(e){res.status(500).json({error:'Database error'})}};
