const {initDb,githubUsers,verifyStoredPassword,ensureUserRecord,createSession,setCookie}=require('../../lib');module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{await initDb();const {username,password}=req.body||{};const {users}=await githubUsers();const u=users.find(x=>x&&String(x.username||'').toLowerCase()===String(username||'').trim().toLowerCase()&&['owner','admin'].includes(x.role)&&x.active!==false);if(!u||!await verifyStoredPassword(password,u.password_hash))return res.status(401).json({error:'Invalid credentials'});const db=await ensureUserRecord(u);const sid=await createSession(db.id);setCookie(res,'zxv_session',sid,60*60*24*7);res.json({ok:true,user:{username:db.username,role:db.role,balance:Number(db.balance)}})}catch(e){console.error(e);res.status(500).json({error:'Login failed'})}};
